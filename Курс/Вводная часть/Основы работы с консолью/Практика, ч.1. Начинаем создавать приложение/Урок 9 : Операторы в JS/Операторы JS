Операторы JS:

Сложение 
Вычитание 
Умножение
Деление


Сложение:

+ - канкатинация при сложении числа и строки будет строка
Унарный плюс - Если перед строкой поставить + мы получим числовое значение.
Инкремент/Дикремент - может быть в префиксной и постфиксной форме. Увеличивает и уменьшает на 1.

let incr = 10,
    decr = 10;

    incr++; - увеличение на 1 
    decr--; - уменьшение на 1

    ++N - префиксная
    N-- - постфиксная

    console.log(incr++); - Отобразит 10 но увеличит 
    console.log(++incr); - Отобразит 11

% Остаток от деления 2-ух чисел 

console.log(5%2); - остаток 1

== - равенство для сравнения двух значений

console.log(2*4 == 8); - результат true
console.log(2*4 == '8'); - результат true (сравнение значения, не типа)
console.log(2*4 === '8'); - результат false (строгое сравнение как значения так и типа данных)
console.log(2*4 === 8); - результат true (строгое сравнение как значения так и типа данных)
&& - логическое И (true && true == true) правда если все правда
|| - логическое ИЛИ (true || false == true) правда если один из правда
! - оператор отрицания
!= - Не равен

Порядок выполнения математики

2 + 2 * 6 === 8 - false

Побитовые операторы.

