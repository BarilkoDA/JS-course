Интерполяция - Позволяет внутри строки устанавливать значение переменной.

const category = 'toys';

console.log('https://someurl.com/' + category); - канкатенация строк.

Для использования интерполяции используются `` - бэктики

console.log(`https://someurl.com/${category}/5`)

Позволяет бинамически использовать переменные.