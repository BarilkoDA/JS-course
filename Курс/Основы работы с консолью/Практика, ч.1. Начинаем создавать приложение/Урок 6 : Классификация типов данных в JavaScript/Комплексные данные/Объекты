Объект - коллекция данных.
Структура которая используется для хранения любых данных

const obj = {

}

Объявляется через  {}

Внитри объекта описание идет через 

key: value,
последнее свойство без запятой.

const obj = {
    key: value,
    name: "Jhon",
    age: 25,
    isMarriedL false
}

Вызов

Console.log(obj.name); - значение свойства name
Console.log(obj["name"]);

---

Массив - это частный случай объекта
Является комплексным типом данных.
Может включать в себя любой тип данных включая объекты и массивы.
Хранение данных строго по порядку.

let arr = ['plum.png', 'orange.jpg', 6, 'apple.bmp', {}, []];

Вызов

Console.log(arr[0]); - первый элемент массива