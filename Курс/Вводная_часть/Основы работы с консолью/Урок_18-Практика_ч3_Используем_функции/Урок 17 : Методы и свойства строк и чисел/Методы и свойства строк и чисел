Методы - вспомогательные функции.
Свойства - вспомогательнае значеия.

свойство: length

const str = "test";
console.log(str.length); // - выводит 4 - количество симфолов.

const arr = [1, 2, 4]];
console.log(arr.length); // - выводит 3 - количество 'ktvtynjd vfccbdf'.

Все методы и свойства можно найти в официальной документации
https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/String

Методы всегда используются со скобками ()

console.dir(Number); - все значения можно также получить воспользовавшись консолью

Методы для строки

Со строкой можно работать как с массивом

const str = "test";
console.log(str[2]); // - получим значение 's'

но при этом мы не можем изменить конкретный символ в строке

const str = "test";
str[2] = 'q';
console.log(str); // - получим значение test

Использование метада не изменяет значение, оно возвращает новое значение

const str = "teSt";

console.log(str.toUpperCase()); // - получим значение TEST
console.log(str.toLowerCase()); // - получим значение test
console.log(str); // - получим значение teSt

Поиск подстроки

const fruit = "Some fruit";

console.log(fruit.indexOf("fruit")); // - получим значение 5 слово fruit начинается с 5 значение (6 символ - но начинается с 0)

---

slice - получить значение из строки

const fruit = "Some fruit";

console.log(fruit.indexOf("q")); // - получим значение -1 символ q не найден

3 метода взаимодействия со строками

const logg = "Hello world";

console.log(logg.slice(6, 11)); - вырезание кусочка начиная с и заканчивая по но не включая.
console.log(logg.slice(6)); - вырезание кусочка начиная с 6 и до конца.
console.log(logg.slice(-5 -1)); - вырезание кусочка начиная с 5 справа и заканцивая 1 с права 

substring - получить значение из строки не поддерживает отрицательные значения

substr - получить значение из строки вместо второго значения конца мы указываем количество символов

----

###  Числа ###

Math. - специальная библиотека для работы с числами

const num = 12.2;
console.log(Math.round(num)); - округление до целого

const test = "12.2px";
console.log(parsInt(test)); - получим 12
                              метод преобразует в число (переводит цисло в другую систему исчисления)
console.log(parsFloat(test)); - получим 12.2
